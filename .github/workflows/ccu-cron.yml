name: CCU Cron

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - name: Capture CCU snapshot
        run: |
          curl --fail-with-body --silent --show-error --max-time 290 \
            --retry 2 --retry-delay 30 \
            --header "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            https://bloxinsights.vercel.app/api/cron/ccu
